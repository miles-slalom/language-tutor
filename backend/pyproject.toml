[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "french-tutor-backend"
version = "0.1.0"
description = "French Tutor Backend API"
requires-python = ">=3.9"
