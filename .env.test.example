# Integration Test Environment Variables
# Copy this file to .env.test and fill in your values

# API Configuration
# The deployed backend API URL
TEST_API_URL=https://vgxcb7g6al.execute-api.us-east-1.amazonaws.com

# Frontend Configuration
# The deployed frontend CloudFront URL
TEST_APP_URL=https://d3blyys2t3r0hm.cloudfront.net

# Authentication (Optional)
# Required for authenticated test cases
# If not set, authenticated tests will be skipped
TEST_USER_EMAIL=
TEST_USER_PASSWORD=

# Cognito Configuration (Optional)
# Required for backend authenticated tests if using direct Cognito auth
# TEST_COGNITO_CLIENT_ID=

# Test Options
# Set to 'true' to run tests in headless mode (default)
# HEADLESS=true

# CI/CD Configuration
# Set to 'true' when running in CI environment
# CI=false
